# Live2D 动作映射指南

换一个 Live2D 模型后，桌宠的"哪种情绪播哪个动作"这件事需要重新调一下。这份文档讲清楚映射机制，以及怎么手动改。

---

## 1. `motion_map.json` 是什么

每个 Live2D 模型目录下（跟 `xxx.model3.json` 同级）都有一个 `motion_map.json`。它是"情绪 → 动作组名列表"的映射表，例如：

```json
{
  "happy": ["w-cute01-glad", "w-cute02-wink", "face_smile_01"],
  "angry": ["w-normal01-shakehead", "face_serious_01"],
  ...
}
```

**首次加载新模型时，程序会自动生成一份**（`pet_renderer.py` 里的 `_generate_motion_map`），按动作组名里的关键词猜它属于哪种情绪。之后每次加载都读这份文件——**你手动改过之后，程序不会覆盖它**，除非把这个文件删掉重新生成。

情绪桶一共 8 个：`happy` `sad` `angry` `shy` `sleepy` `tease` `curious` `neutral`。

---

## 2. 三层匹配逻辑

收到一句 AI 回复（或触发待机独白）时，程序按这个顺序决定播什么动作：

1. **`[emotion:xxx]` 标签**——如果人设 prompt 让 AI 在回复里带这种标签，直接用标签指定的情绪，最准。
2. **具体动作词**（`guess_specific_action`，仅用于对话回复，不用于待机）——在回复文字里找"点头""摇头""叹气""揉眼睛"这类具体动作词，命中就直接定位到那个动作，不走情绪桶随机。关键词表在 `pet_renderer.py` 的 `_ACTION_KEYWORDS`。
3. **情绪关键词兜底**（`guess_mood_from_text`）——前两层都没命中，就从文字里数"害羞""难过""开心"之类的关键词，选命中最多的情绪桶，随机抽一个动作。全都没命中就落到 `neutral`（不会纯随机瞎猜）。

待机时（3 分钟无操作、主动搭话）只会用 `neutral` / `sleepy` 这两个桶，不会触发对话相关的情绪判断。

---

## 3. 换模型后要检查什么

新模型第一次加载会自动生成 `motion_map.json`，但自动分桶只是"按关键词猜"，通常有三类问题：

**A. 分桶内容不准**

比如自动生成的 `angry` 桶里混了几个表情特别夸张的动作组，看起来像"惊恐"而不是"生气"。这种要打开模型目录下的 `motion_map.json`，把不合适的组名从列表里删掉。

怎么找到某个动作组具体是什么样子：去模型目录的 `motions/` 文件夹，文件名通常能看出动作类型（比如 `w-cool10-angry.motion3.json`、`face_serious_01.motion3.json`）。不确定的话，跑起来玩几次，观察哪个动作/表情播出来后你想去掉，记下组名再去 `motion_map.json` 里删。

**B. 具体动作词匹配不上**

`_ACTION_KEYWORDS` 依赖动作组名的**命名规律**——现在项目适配的是 Project Sekai 官方模型的命名习惯，即 `w-场景编号-动作类型`（比如 `w-adult01-nod`，取最后一段 `nod` 作为动作类型）。

如果新模型用完全不同的命名方式（比如官方常见的 `TapBody` / `FlickHead` / `Idle_01`），这层会一直匹配不到，自动回退到第 3 层情绪桶随机——不会报错，只是动作会变得不够精确，跟话对不太上具体细节（但情绪大方向还是对的）。

想让新模型也支持精确动作词匹配，两个办法：
- 改 `pet_renderer.py` 里的 `_ACTION_KEYWORDS`，把 value 换成新模型对应的组名片段
- 或者改 `MotionPicker.pick_specific` 里的命名解析规则（`tail.rsplit("-", 1)[-1]` 这段），适配新模型自己的命名格式

**C. 待机时没有动作，停在大字型/绑定姿势**

如果模型没有专门的 `Idle` 动作组，`initializeGL` 会从 `neutral` 桶里随机挑一个动作作为启动姿势（见 `pet_renderer.py` 里 `initializeGL` 方法的 `else` 分支）。如果 `neutral` 桶本身是空的（自动分桶没猜中任何动作），启动时还是会停在默认姿势——这时候需要去 `motion_map.json` 手动往 `neutral` 里加几个组名。

---

## 4. 手动改 `motion_map.json` 的步骤

1. 启动一次桌宠，确认新模型目录下已经生成了 `motion_map.json`
2. 用文本编辑器打开这个文件（就是普通 JSON，8 个情绪键，每个对应一个组名数组）
3. 想调整某个情绪：
   - 看着不对的动作/表情，把组名从列表里删掉
   - 想加新的，从模型的 `motions/` 目录里找到合适的组名（去掉 `.motion3.json` 后缀），加进对应的情绪数组
4. 保存文件，重启桌宠——不用重新生成，程序直接读你改过的版本

**小技巧**：`w-` 开头的是身体动作，`face_` 开头的是纯表情动作，两种都可以往同一个情绪桶里加，程序会自动分开处理（对话/待机时身体动作和表情是分别挑的，不会互相覆盖）。

---

## 5. 参考：本项目奏模型的调整记录

`D:\live2d\v2_17kanade_april2026_t01\motion_map.json` 目前手动做过这些调整，换模型时可以参考思路：

- **`angry` 桶精简**：去掉了专属"生气"动作和 `face_angry_01~04`（表情过于夸张），只留摇头系动作，表情换成更克制的 `face_serious_01~04`
- **`sleepy` 桶去掉 `dizzy` 动作**：`w-special02-dizzy01`（晕乎乎/眼前发黑）容易被误认成"不舒服"而不是"困"，删掉了
- **启动姿势兜底**：这个模型没有 `Idle` 分组，靠上面第 3 条的 `neutral` 兜底逻辑避免停在大字型

